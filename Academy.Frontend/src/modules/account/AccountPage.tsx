import {
  Drawer,
  ListItem,
  ListItemButton,
  ListItemIcon,
  ListItemText,
} from "@mui/material";

export function AccountPage() {
  return (
    <Drawer open>
      <ListItem>
        <ListItemButton>
          <ListItemIcon></ListItemIcon>
          <ListItemText primary={"csdcdscds"} />
        </ListItemButton>
      </ListItem>
    </Drawer>
  );
}
